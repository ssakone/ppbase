Release wheels place the PostgreSQL, OpenSSL, and zlib license texts in this
directory. PostgreSQL's text comes from its checksum-verified source tree,
OpenSSL's from the build dependency used for that platform, and zlib's from
the canonical checked-in notice used by the recorded wheel build.
